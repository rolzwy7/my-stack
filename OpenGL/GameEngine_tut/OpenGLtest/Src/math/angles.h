#pragma once

#define _USE_MATH_DEFINES
#include <math.h>

namespace engine {
	namespace math {
	
		float to_radians(float degrees);
		float to_degrees(float radians);

	} // namespace math
} // namespace engine
